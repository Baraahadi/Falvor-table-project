
    //   const response = await pool.query("SELECT * FROM recipes LIMIT 3");
    // res.json(response.rows); // return as JSON
    // const response = await axios.get(
    //   "https://api.spoonacular.com/recipes/random",
    //   {
    //     params: {
    //       apiKey: API_KEY,
    //       number: 6,
    //     },
    //   }
    // ); // here the logic of fetching random recipes from the given API with API_KEY // response = data object that contain recipes object that have array of details inside
    // // res.send(response.data)
    // // const randomRecipes =await response.json();

    // const randomRecipes = await response.data.recipes || [] ; // save the recipes result here
    // const result =randomRecipes.map(e=>`Title : ${e.title}, Image:  ${e.image} , Instructions: ${e.instructions}, Ingredients: ${e.ingredients}`)
//   if (randomRecipes){
// localStorage.setItem('recipes', JSON.stringify(randomRecipes));
//   }

/////////////////////////////////////////////////////////////////
